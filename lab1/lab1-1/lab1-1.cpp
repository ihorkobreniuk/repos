
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
using namespace std;

int main()
{
	int a, b, c, d, e, f;

	a = sizeof(int);
	b = sizeof(short);
	c = sizeof(long);
	d = sizeof(char);
	e = sizeof(float);
	f = sizeof(double);
	//sizeof рассчитывает кол-во байт в операторе

	return 0;
}
//f11 - Шаг с заходом. f10 - шаг с обходом. f5 - с отладкой. ctrl + f5 - без отладки